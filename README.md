# AWS Dynamic CDN Terraform module

Terraform module which creates the resources to host a dynamic CDN domain on AWS.

## Usage

```hcl
module "main" {
  source      = "genstackio/layer-dyncdn/aws"
  
  // ...
}
```
